# requirements-dev.txt — dependencias de desarrollo
# Instalar con: pip install -r requirements-dev.txt
 
# Tests
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23       # si usas async
pytest-mock>=3.12          # mocking cómodo
 
# Linting & formato
ruff>=0.4
black>=24.0
isort>=5.13
mypy>=1.9
 
# Seguridad
bandit[toml]>=1.7
pip-audit>=2.7
safety>=3.0
 
# Pre-commit
pre-commit>=3.7